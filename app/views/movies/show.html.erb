
<div class="ui container">
  <div class="embed-container">
    <iframe src="<%= @movie.trailer %>" frameborder="0" allowfullscreen></iframe>
  </div>
  <h1 class="ui header"><%= @movie.title %></h1>
</div>
<br />
<h5>Duration</h5>
<%= @movie.duration %>
<br />
<h5>Genre</h5>
<%= @movie.genre %>
<br /> 
<a href="<%= @movie.description %>">IMDB Description</a>
<br />
<h5>Last Updated</h5>
<%= @movie.updated_at %>
<br />
<h5>Created</h5>
<%= @movie.created_at %>
<h5>Comments</h5>
<% if user_signed_in? %>
  <%= link_to 'Add Comment', new_movie_comment_path(@movie) %>
<% else %>
  <%= link_to 'Sign in to add a comment', new_user_session_path %>
<% end %>
<div class="ui comments">
  <% @movie.comments.reverse.each do |comment| %>
    <%= render 'comments/comment', { comment: comment }  %>
  <%end%>
</ul>
